{{input value=newName}}
<button {{action "addUser"}}>Add User</button>
<ul>
  {{#each users as |user|}}
      <li>
        {{user}}
      </li>
  {{/each}}
</ul>
<button {{action "saveUsers"}}> Save </button>
